{
   "name": "calc",
   "version": "1.0.0",
   "description": "Simple calculator app",
   "scripts": {
     "start": "http-server -p 8080",
     "test": "echo \"Error: no test specified\" && exit 1"
   },
   "dependencies": {
     "http-server": "^0.12.3"
   }
 }